Put your ringing sound file here, named exactly:

    ringing.mp3

It should be a single phone ring (the sound just once, not already looped) -
the app loops it automatically while a call is dialing out. Once you add the
real file, you can delete this placeholder (README.txt) if you like; it's
only here so the empty folder still gets committed to git.
